<div class="jumbotron">
	<h1 align="center">Welcome!</h1>
	<% if user_signed_in? %>
		
	<% else %>
		<p align="center">Sign up <%= link_to "Here", new_user_registration_path, class: "btn btn-primary" %> or <%= link_to "Login", new_user_session_path, class: "btn btn-primary" %> 
		</p>
	<% end %>
</div>
